<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="using:DnDClient.ViewModels"
             x:Class="DnDClient.Views.ProfilePage">
    <ContentPage.BindingContext>
        <viewModels:ProfileViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid RowDefinitions="*,*,*,*,*">
            <Entry Placeholder="Имя пользователя" Text="{Binding NewUsername}" Grid.Row="0"></Entry>
            <Button Text="Сменить имя пользователя" Grid.Row="1" Command="{Binding ChangeUsernameCommand}" />
            <Entry Placeholder="Новый пароль" Grid.Row="2" IsPassword="True" Text="{Binding NewPassword}"></Entry>
            <Entry Placeholder="Подтверрждение нового пароля" Grid.Row="3" IsPassword="True"
                   Text="{Binding CheckNewPassword}">
            </Entry>
            <Button Text="Сменить пароль" Grid.Row="4" Command="{Binding ChangePasswordCommand}"></Button>
        </Grid>
    </ContentPage.Content>
</ContentPage>