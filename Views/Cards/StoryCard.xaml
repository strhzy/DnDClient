<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="using:DnDClient.ViewModels"
             x:Class="DnDClient.Views.Cards.StoryCard">
    <Frame Margin="5" Padding="10" BorderColor="Gray" CornerRadius="10">
        <StackLayout>
            <Entry Text="{Binding Name, Mode=TwoWay}" Placeholder="Название сюжета" Completed="OnNameCompleted"/>
            <Editor Text="{Binding Description, Mode=TwoWay}" Placeholder="Описание" Completed="OnDescriptionCompleted"/>
            <Button Text="Удалить" Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CampaignViewModel}}, Path=DeleteStoryCommand}" CommandParameter="{Binding .}" BackgroundColor="DarkRed" TextColor="White" Margin="0,5,0,0"/>
        </StackLayout>
    </Frame>
</ContentView>